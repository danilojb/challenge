apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: tech-challenge
  labels:
    app: evaluation-service
spec:
  type: ClusterIP
  selector:
    app: evaluation-service
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  sessionAffinity: None
