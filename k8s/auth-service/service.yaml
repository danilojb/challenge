apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: tech-challenge
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  sessionAffinity: None
