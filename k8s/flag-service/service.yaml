apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: tech-challenge
  labels:
    app: flag-service
spec:
  type: ClusterIP
  selector:
    app: flag-service
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  sessionAffinity: None
